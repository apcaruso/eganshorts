# Egan stories

Minimal static index for the bundled story collections.

```bash
python3 -m http.server ${PORT:-8000}
```

Open `http://localhost:${PORT:-8000}`. Deploy by enabling GitHub Pages for the repo root.
